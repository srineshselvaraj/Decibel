import Rooms from "@/components/rooms";
import { View } from "react-native";

export default function RoomsPage() {
  return (
    <View style={{ flex: 1, backgroundColor: "#1a0f2e" }}>
      <Rooms />
    </View>
  );
}
